import Body from "../body/body";
import Navigator from "../navigation/navigator";

const Main = () => {
    return (
        <div className="h-full">
            <Navigator />
            <Body />
        </div>
    );
};

export default Main;
